import {Grid} from "@mui/material";

const styles = {
    backgroundColor: '#000',
    maxWidth: '80%',
    justifyContent: 'center',
    padding: 25,
    display: 'flex',
}

const NavBar = () => {
    return (
        <Grid style={styles}>
            kacsa
        </Grid>
    );
};

export default NavBar;

